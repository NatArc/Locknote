/* == Existing CSS == */

/* Global Layout */
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
    font-family: "Poppins", sans-serif;
}

html, body {
    width: 100%;
    height: 100%;
}

body {
    background: #f5f8ff;
    height: 100vh;
}
.locknote-title {
    font-size: 48px;      
    color: #6a5cff;       
    font-weight: 700;     
    margin-bottom: 20px;  
    text-align: center;   
    text-shadow: 1px 1px 3px rgba(0,0,0,0.5); 
}
/* Top-right controls (logout + account) */
.top-right {
    position: absolute;
    top: 18px;
    right: 18px;
    display: flex;
    gap: 8px;
    z-index: 40;
}

.logout-btn, .account-btn {
    border: none;
    padding: 8px 12px;
    border-radius: 8px;
    font-weight: 600;
    cursor: pointer;
    box-shadow: 0 2px 6px rgba(0,0,0,0.12);
}

.logout-btn {
    background: red;
    color: #fff;
}

.account-btn {
    background: #fff;
    color: #333;
    border: 1px solid #ddd;
}

/* Centered container only for sign in / sign up / account box */
.container {
    width: 100%;
    max-width: 420px;
    margin: 0 auto;
    padding-top: 60px;
}

.form-box {
    background: #fff;
    padding: 40px 35px;
    border-radius: 20px;
    box-shadow: 0px 5px 25px rgba(0,0,0,0.08);
    text-align: center;
}

.icon {
    font-size: 40px;
    margin-bottom: 10px;
}

h2 {
    font-size: 28px;
    font-weight: 600;
    margin-bottom: 10px;
}

.subtitle {
    font-size: 14px;
    color: #777;
    margin-bottom: 25px;
}

label {
    font-size: 14px;
    display: block;
    text-align: left;
    margin-bottom: 6px;
}

.input-box {
    margin-bottom: 15px;
    position: relative;
}

.input-box input {
    width: 100%;
    padding: 12px 15px;
    border-radius: 10px;
    border: 1px solid #ccc;
    outline: none;
    font-size: 14px;
}

.input-box input:focus {
    border-color: #6a5cff;
}

.password-box input {
    padding-right: 40px;
}

.toggle-password {
    position: absolute;
    right: 10px;
    top: 50%;
    transform: translateY(-50%);
    background: none;
    border: none;
    cursor: pointer;
    font-size: 18px;
}

.forgot {
    text-align: right;
    display: block;
    margin-bottom: 20px;
    color: #6a5cff;
    text-decoration: none;
}

.btn {
    width: 100%;
    padding: 12px;
    border-radius: 10px;
    border: none;
    background: linear-gradient(90deg, #2d76ff, #6a5cff);
    color: #fff;
    font-size: 16px;
    cursor: pointer;
}

.btn.alt {
    background: #f0f0f0;
    color: #333;
}

.btn.primary {
    background: linear-gradient(90deg,#2d76ff,#6a5cff);
    color: #fff;
}

/* Signup Steps */
.steps {
    display: flex;
    justify-content: space-between;
    margin-bottom: 20px;
}

.step {
    flex: 1;
    text-align: center;
    color: #777;
}

.step.active {
    color: #6a5cff;
    font-weight: bold;
}

.progress-bar {
    background: #e0e0e0;
    height: 4px;
    border-radius: 2px;
    margin-bottom: 20px;
}

.progress-fill {
    background: linear-gradient(90deg, #2d76ff, #6a5cff);
    height: 100%;
    width: 33%;
    border-radius: 2px;
    transition: width 0.3s ease;
}

.step-content {
    display: none;
}

.step-content.active {
    display: block;
}

.error {
    color: red;
    font-size: 12px;
    margin-top: -10px;
    margin-bottom: 10px;
}

.info-msg {
    color: green;
    font-size: 12px;
    margin-top: -6px;
    margin-bottom: 6px;
}

.password-req li {
    list-style: none;
    font-size: 12px;
    color: red;
}

.password-req li.valid {
    color: green;
}

.review-item {
    background: #f7f8ff;
    padding: 12px;
    border-radius: 12px;
    display: flex;
    align-items: center;
    margin-bottom: 10px;
}

.review-item .icon {
    font-size: 20px;
    margin-right: 10px;
}

.review-item .text {
    text-align: left;
}

/* Hello Page (Full screen view) */
#helloView {
    position: relative;
    width: 100%;
    height: 100vh;
    display: flex;
    justify-content: center;
    align-items: center;
}

.hello-page {
    font-size: 48px;
    color: #6a5cff;
}

/* Views */
.view {
    display: none !important; /* Hide all screens */
    width: 100%;
    height: 100%;
}

.view.active {
    display: block !important; /* Show only active screen */
}

/* Modal */
.modal {
    display: none;
    position: fixed;
    inset: 0;
    background: rgba(0,0,0,0.45);
    justify-content: center;
    align-items: center;
    z-index: 60;
}

.modal-content {
    width: 100%;
    max-width: 420px;
    background: #fff;
    padding: 22px;
    border-radius: 12px;
    box-shadow: 0 8px 30px rgba(0,0,0,0.2);
}

.modal-content h3 {
    margin-bottom: 10px;
}

.modal-actions {
    margin-top: 12px;
    display: flex;
    gap: 10px;
}

.back-left {
    position: absolute;
    top: 18px;
    left: 18px;
    z-index: 40;
}

.back-btn {
    border: none;
    padding: 8px 12px;
    border-radius: 8px;
    background: #fff;
    color: #333;
    border: 1px solid #ddd;
    font-weight: 600;
    cursor: pointer;
    box-shadow: 0 2px 6px rgba(0,0,0,0.12);
}

/* Hello Page (Full screen view) */
#helloView {
    position: relative;
    width: 100%;
    height: 100vh;
}

.hello-page {
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    font-size: 48px;
    color: #6a5cff;
    text-align: center;
}

#toSignin,
#toSignup {
    text-decoration: none !important;
}
.bottom-text {
    margin-top: 12px; 
}
.hello-datetime {
    position: absolute;
    bottom: 18px;
    right: 18px;
    font-size: 14px;
    color: #555;
    background: rgba(255, 255, 255, 0.7);
    padding: 6px 10px;
    border-radius: 6px;
    box-shadow: 0 2px 6px rgba(0,0,0,0.12);
    font-weight: 500;
}

/* ================= Notes System CSS ================= */

/* Notes Button (bottom-left) */
.notes-btn {
    position: fixed;
    bottom: 18px;
    left: 18px;
    background: #fff;
    border: 1px solid #ddd;
    border-radius: 8px;
    padding: 10px 16px;
    font-weight: 600;
    cursor: pointer;
    box-shadow: 0 2px 6px rgba(0,0,0,0.12);
    z-index: 50;
    transition: background 0.2s;
}

.notes-btn:hover {
    background: #f0f0f0;
}

/* Notes Slide Panel */
.notes-panel {
    position: fixed;
    top: 0;
    left: -320px; /* hidden initially */
    width: 300px;
    height: 100%;
    background: #fff;
    box-shadow: 2px 0 12px rgba(0,0,0,0.15);
    padding: 20px;
    z-index: 55;
    overflow-y: auto;
    transition: left 0.3s ease;
}

.notes-panel.active {
    left: 0; /* slide in */
}

/* Notes Header */
.notes-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 12px;
}

.notes-header h3 {
    font-size: 18px;
    font-weight: 600;
}

.close-notes {
    border: none;
    background: none;
    font-size: 20px;
    cursor: pointer;
}

/* Add Note Button inside panel */
.add-note-btn {
    width: 100%;
    padding: 10px;
    margin-bottom: 15px;
    border: 1px solid #6a5cff;
    border-radius: 8px;
    background: #fff;
    color: #6a5cff;
    font-weight: 600;
    cursor: pointer;
    transition: all 0.2s ease;
}

.add-note-btn:hover {
    background: #f0f0ff;
}

/* Notes List */
.notes-list {
    display: flex;
    flex-direction: column;
    gap: 10px;
}

/* Individual Note Card (Simple & Minimal) */
.note-card {
    background: #f7f8ff;
    border-radius: 10px;
    padding: 10px;
    font-size: 14px;
    cursor: pointer;
    border: 1px solid #e0e0e0;
}

.note-card h4 {
    font-weight: 600;
    margin-bottom: 4px;
}

.note-card p {
    font-size: 13px;
    color: #555;
    overflow: hidden;
    white-space: nowrap;
    text-overflow: ellipsis;
}

/* Add/Edit Note Modal (reuse existing modal style) */
.note-modal textarea.note-textarea {
    width: 100%;
    height: 100px;
    padding: 10px;
    font-size: 14px;
    border-radius: 8px;
    border: 1px solid #ccc;
    resize: none;
}
